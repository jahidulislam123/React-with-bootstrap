import React from 'react';
import { CardGroup } from 'react-bootstrap';

const Cart = (props) => {
    const { name, price } = props.product;
    return (
        <div>


        </div>
    );
};

export default Cart;